# US Fresh Jobs Platform

A continuously updated U.S. job data platform. Not a website with 100 jobs — the database
preserves every qualifying job, and the UI provides fast access to the newest and most
relevant ones.

**Current state: milestones 1–11 and 14 are built and running on live data.** Jobs flow
from the source bucket into PostgreSQL, out through the API, and onto a rendered Next.js
UI, on a daily schedule. Authentication, alerts and monitoring are not built. The
production stack is written but has not yet been run on a server — see
[`docs/07-deployment.md`](docs/07-deployment.md).

---

## Read this first

[`docs/00-source-verification.md`](docs/00-source-verification.md) records what was
**measured** about the OpenJobData source, not assumed. Several findings changed the
architecture:

- The `minimal` dataset variant has **no city, state, salary or description** — only a
  free-text `country`. The `full` variant is therefore mandatory.
- Excluding the `entire_json` column via Parquet column projection **halves** the daily
  download: 238 MB → 120 MB.
- `posted_at` is **NULL for 19.3%** of rows and contains **real future dates** (observed
  34 days ahead). It can never be used naively for "posted 18 minutes ago".
- The publisher **skips days** — and the gap sets differ between variants. Discovery must
  diff a directory listing, never iterate a date range.
- Publication is **same-day** but the upload hour varies (06:33–14:15 UTC), so a fixed-time
  cron would sometimes run before the day's file exists.
- The `state` field carries **non-US values** (`Quebec`, `Maharashtra`), so a populated
  state never implies a US job.
- A single day's delta carries **~62,000 U.S. rows**, which is why ingestion has no row cap.

Re-verify at any time — the source can change:

```bash
python scripts/verify_source.py
```

---

## Quick start

```bash
make init            # create .env.development from the template
make up              # postgres + redis + minio + api
make migrate         # apply the database schema
make test            # 492 tests
make layering        # source-isolation guard
```

Then: <http://localhost:8000/docs>

Without Docker, point `DATABASE_URL` at a local PostgreSQL 17 and run `make migrate && make test`.

`make help` lists all targets.

---

## The daily schedule

The source publishes one delta file per day, but the upload hour varies across roughly
06:33–14:15 UTC — so a single fixed slot regularly runs before the day's file exists. The
schedule is therefore a primary run plus cheap re-checks:

| | When (America/New_York) | Does |
|---|---|---|
| `jobplatform-daily` | **09:00** | ingest, then enforce the freshness window |
| `jobplatform-catchup` | 11:00, 13:00, 15:00, 17:00 | ingest **only if today's file is still missing** |

A catch-up that finds the day already done costs one indexed query — no listing, no
download. Install both timers on a server with `sudo ./scripts/install-systemd.sh`; run
either by hand with `make daily` or `make catch-up`, and ask the question directly with
`make have-today`. Full detail, including the cron equivalent:
[`docs/07-deployment.md`](docs/07-deployment.md) §3.

---

## Architecture

```
HF bucket ──► SourceConnector ──► object storage ──► ingestion worker ──► PostgreSQL
                                                            │                 │
                                                     validate/normalize    FastAPI
                                                     locate/dedupe            │
                                                     lifecycle             Next.js
```

Full detail, ERD, security model and deployment stages:
[`docs/01-architecture.md`](docs/01-architecture.md).

**Stack.** Next.js + TypeScript + Tailwind · FastAPI + Pydantic · Polars/PyArrow ·
PostgreSQL 17 · Redis · Celery · S3-compatible storage · Docker.

**The rule that keeps sources swappable:** all OpenJobData knowledge lives inside
`workers/ingestion/ingestion/connectors/openjobdata/`. CI fails the build if `openjobdata`,
`huggingface` or `hf://` appears anywhere else — run `make layering` to check locally. It
caught three real leaks the day it was added.

---

## Layout

```
apps/api/           FastAPI service
apps/web/           Next.js app (Milestone 11)
workers/ingestion/  connectors, pipeline, tasks
packages/shared/    config, logging, db, time, errors
packages/schemas/   Pydantic contracts, enums, US reference data
database/migrations/ Alembic — hand-authored, never autogenerated
infra/docker/       Dockerfiles and Postgres init
scripts/            verify_source.py and operational tooling
docs/               source verification, architecture, milestones
```

---

## Milestones

| # | Milestone | Status |
|---|---|---|
| 1 | Foundation: repo, Docker, Postgres, migrations, health API, CI | **Complete** |
| 2 | Object storage + `SourceConnector` Protocol + sync tracking | **Complete** |
| 3 | `OpenJobDataConnector`: discover, fetch, validate, normalize | **Complete** |
| 4 | Parquet pipeline: Polars batches, rejects to `sync_errors` | **Complete** |
| 5 | `LocationNormalizer` + US classification | **Complete** |
| 6 | Deduplication L1–L4 | **Complete** |
| 7 | Lifecycle, `job_events`, `FreshnessService` | **Complete** |
| 8 | Full ingestion task, idempotency, crash recovery | **Complete** ¹ |
| 9 | FastAPI v1 read endpoints + cursor pagination | **Complete** |
| 10 | PostgreSQL search + `SearchService` abstraction | **Complete** ² |
| 11 | Next.js feed and job detail | **Complete** |
| 12 | Auth, saved jobs, saved searches | Not started |
| 13 | Alerts + notification outbox | Not started |
| 14 | Admin dashboard | **Built, unauthenticated** ³ |
| 15 | Monitoring and alerting | Not started |
| 16 | Production deployment | Written, not yet run ⁴ |

¹ Ingestion runs from `scripts/ingest.py` on a schedule, not as a Celery task —
`ingestion/tasks/` is still a placeholder. Idempotency and crash recovery are built and
tested: `job_sources (source, external_job_id)` is a database UNIQUE, and the stale
`RUNNING` row a killed run leaves behind is reclaimed after 120 minutes.

² Full-text search is real PostgreSQL — a `search_vector` column queried with
`websearch_to_tsquery` and ranked with `ts_rank`. It lives in the read repository; there
is no separate `SearchService` class yet, so swapping the engine would mean touching that
repository.

³ `/admin` and `/api/v1/admin/ingestion` expose ingestion aggregates. Both are public —
they move behind the admin role when milestone 12 lands.

⁴ Compose files, Dockerfiles, Caddy config and the systemd timers are all in the repo and
statically validated, but have never been built or run: the development machine's Docker
engine does not start. Treat the first deploy as the real test.

Beyond the original list, role filtering and classification (category, seniority,
industry) are built and documented in
[`docs/06-role-filtering.md`](docs/06-role-filtering.md).

Per-milestone detail: [`docs/02-milestone-1.md`](docs/02-milestone-1.md),
[`docs/03-milestone-2.md`](docs/03-milestone-2.md),
[`docs/04-milestone-3.md`](docs/04-milestone-3.md).

---

## Data principles

These are enforced in code and in the database, not just documented:

1. **`posted_at` is never overwritten by `first_seen_at`.** Both are shown, labelled, in
   the UI. Timestamps are never fabricated.
2. **Seven distinct timestamps** — `posted_at`, `first_seen_at`, `last_seen_at`,
   `last_updated_at`, `source_fetched_at`, `close_at`, `closed_at`. Collapsing any two
   loses information the source actually provides.
3. **Jobs are never deleted.** They transition between `ACTIVE`, `EXPIRED`, `REMOVED` and
   `UNKNOWN`, and every transition is recorded in `job_events`.
4. **Nothing is silently discarded.** Every rejected row is stored in `sync_errors` with a
   reason from a closed enum of 14.
5. **Ingestion is idempotent.** Reprocessing a file cannot create duplicates —
   `job_sources (source, external_job_id)` is a database UNIQUE.
6. **No artificial limits.** If a day has 62,000 qualifying U.S. jobs, all 62,000 are stored.

---

## Licensing

The OpenJobData dataset declares **MIT** (verified in the bucket `README.md`). That covers
the dataset artifact; it does not by itself grant rights over third-party job content,
employer trademarks, or downstream ATS terms of service. **Legal review of re-display
rights is required before public launch** — tracked as item 7 in
[`docs/00-source-verification.md`](docs/00-source-verification.md) §10.
